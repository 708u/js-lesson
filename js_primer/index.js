'use strict'

const obj = {
    name: 'foo',
    hello() {
        return 'hello!';
    }
}

const arr = [
    'foo', 'bar', 'buz'
]

const heyhey = () => 'heyhey';

console.log(`${obj.hello()}, baby`);
// console.log(arr.some(obj => obj.name === 'barr'));

const input = 'bar';
test`foo ${0} ${1}`;
function test(strings) {
    strings.forEach((s) => console.log(s))
}
